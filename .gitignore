# Model weights: ~1.2 GB. Committing one is effectively permanent.
*.gguf

# Optional payload served by /download, mounted at run time.
download.zip

# Local configuration. Holds BITNET_API_KEY, so it must never be committed.
# .env.example is the tracked template.
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local build output
build/
logs/
